# Airflow image dependencies for the DOTA pipeline DAG.
# The DAG uses BashOperator to shell out to the repo's run_pipeline.py, so the
# Airflow container needs dbt + psycopg + python-dotenv + requests installed
# alongside airflow itself.
apache-airflow==2.10.4
apache-airflow-providers-postgres>=5.13
astronomer-cosmos>=1.6,<2
dbt-core~=1.12.0
dbt-postgres~=1.11.0
psycopg[binary]>=3.2,<4
python-dotenv>=1.0,<2
requests>=2.32,<3
