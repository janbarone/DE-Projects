[project]
name = "dota-dagster"
version = "0.1.0"
description = "Dagster orchestration for the DOTA 2 medallion pipeline"
requires-python = ">=3.10"
dependencies = [
    "dagster>=1.9",
    "dagster-webserver>=1.9",
]

[tool.dagster]
module_name = "definitions"
